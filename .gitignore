__pycache__/
*.pyc
.DS_Store

# Rotational exploration scratch. lab.html / presets.html / fades.html ARE tracked
# — they are the sources for published artifacts and would not survive a clone
# otherwise. Only the comparison renders they reference are ignored; those
# regenerate via --sheet / --preset-sheet.
explorations/rotational/*
!explorations/rotational/*.html
